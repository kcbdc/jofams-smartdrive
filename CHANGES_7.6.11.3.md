# 7.6.11.3 (7.6.11.2 이하 포함)

## 7.6.11.3 추가 수정
- 상습정체 구간: 안내 카드/지도 마커 모두 제거 (지도 위 빨간 원+흰 자동차 마커 포함, CSS로도 이중 차단). 좌표가 잘못된 마커가 화면 좌상단에 찍히지 않도록 마커 생성 전 좌표 검증 추가.
- 구간단속 종료 팝업 미표시: 구간단속 구간 안내가 끝나면 그대로 종료. (음성 안내는 유지)
- 운행(경로 안내) 시작 전 차량 캐릭터 고정: GPS 흔들림(반경 max(25m, 정확도×1.6) 이내, 실제 이동속도 미확인)으로는 위치를 갱신하지 않음.
- 단속카메라 위치 정확도:
  - 정의되지 않은 채 호출되던 cameraRouteMatch() 정의(구간단속 노드/카메라 매칭 실패로 보조 카메라가 통째로 누락될 수 있던 문제).
  - nearestPointOnRoute: 긴 경로에서 stride 샘플링 때문에 가장 가까운 구간을 놓치던 문제 수정.
  - 카메라를 경로 정점(vertex)이 아니라 경로 선 위 정확한 투영점(routeDistance, snapLng/snapLat)으로 저장 → 알림 거리·지도 핀 위치가 정점 간격만큼 어긋나던 문제 수정.
  - 알림 거리 = 카메라 routeDistance - 현재 routeDistance (지나간 카메라는 즉시 제외).
  - (테스트: 12,000 정점 경로에서 기존 방식은 카메라 300개 중 105개가 12m 이상 오차, 신규 방식은 최대 1.7m / 정점 2개짜리 1km 직선에서 기존 999m → 신규 300m)
- 터널/GPS 끊김:
  - 알려진 터널 안에서는 추정 주행 3분 제한 해제(최대 25분, 장거리 터널).
  - 추정 속도: 진입 직전 순간속도 대신 최근 속도 지수평균 사용, 해당 도로 제한속도 초과 금지.
  - 전방 터널 650m 전 음성 안내, 터널 안 추정 주행 시작 시 1회 안내, 터널 탈출 시 "위치를 다시 확인했습니다" 안내.
  - 터널 출구 직후 정확도 70m 초과 GPS는 최대 8회 무시(부정확한 좌표로 경로이탈/재탐색 방지).
  - 속도 패널 라벨: 터널 안 "터널 중", 그 외 GPS 추정 "위치추정".

## 7.6.11.2

## 7.6.11.2 추가 수정
- 상습정체 구간 안내 제거 (안내 카드 + 지도 마커).
- 주행 팝업은 동시에 여러 개 뜨지 않고 우선순위에 따라 하나만 표시:
  1순위 구간단속(구간단속 패널, 구간단속 시작/종료 안내) → 2순위 과속주의 → 3순위 신호·과속단속카메라(및 그 외 안전 안내) → 4순위 기본 도로 운행사항(차로 안내)
  - app.js `arbitrateDrivePopups()` 가 #driveView[data-pop] 을 정하고, styles.css 가 나머지를 숨김.
  - 과속주의 팝업은 다른 안내와 같은 상단 자리, 같은 카드 형태로 표시(빨간 계열).
  - 가로 화면에서는 팝업이 떠 있는 동안 주변 최저가 칩을 숨김.

## 7.6.11.1

## 7.6.11.1 추가 수정
- 굽은 도로 / 이중 굽은 도로 안내 카드 제거 (app.js computeSafetyCandidates: 합성 커브 이벤트 삭제, 실데이터 curve_left/curve_right/double_curve 제외)
- 차로 안내("50m 전 직진 차로 · 우회전")를 상단 안내 카드 자리로 이동 (세로/가로 모두).
  차로 안내와 다른 안전 안내(카메라 등)가 동시에 뜨면 차로 안내가 위, 나머지는 그 아래로 자동 배치.

# 7.6.11.0 — 주행 화면 시안(스크린샷 3장) 재현

## 반드시 알아야 할 수정 (버그)
- styles.css 1765·1769·1778·1788·1796·1806·1839번째 줄이 선언 중간에서 잘려 있어(닫는 `}` 7개 누락)
  그 뒤 스타일 전체(7.6.9.x ~ 7.6.10.0 네이비 스타일 포함)가 무효 처리되고 있었음 → 같은 규칙의 온전한 사본으로 복구.
- `.drive-view>*{position:relative}` 삭제 (AR·라디오·하단 카드 등의 position:absolute 를 덮어써 레이아웃을 깨뜨림).

## 스타일 (styles.css)
- 파일 맨 아래 "7.6.11.0" 블록 추가. 시안 폭 941px 기준을 `--u`(화면 폭 비례 단위)로 사용.
- 상단 턴 카드(3줄) · 다음 턴 · 라디오/AR · 교통 레일 · 카메라/상습정체/구간단속 카드
  · 주변 최저가 · 속도 패널(노란 좌측 바, 현재속도/정체) · 지도 컨트롤 · 흰색 원형 버튼 · 하단 3칸 정보 카드.
- 가로 화면 전용 배치(@media orientation:landscape) 추가.

## HTML (index.html)
- 상단 카드 3번째 줄 `#maneuverSub`, 구간단속 패널 구조, 하단 카드 3칸(dbn-cell) 추가.
- Pretendard 웹폰트 링크 추가, styles.css / app.js 버전 7.6.11.0.

## app.js (최소 수정, 각 수정에 `7.6.11.0` 주석 또는 함수 추가)
- setArrivalTime/etaKo : 도착 예정 "오전 11:04" 표기
- guideSubText : 상단 카드 3번째 줄(경유 도로명 + 회전 방향)
- updateTrafficStatus : #speedPanel[data-traffic] (정체/지체/서행 라벨)
- updateSafetyUI : 카메라 카드 문구 "제한속도 60km/h", 제목 "신호·과속 단속카메라"
- safetyLabel : 상습정체 문구 2줄
- 구간단속 값 숫자/단위 분리(<i>), 주유소명 (주)→㈜

## 적용 못 한 것 (지도/JS 영역)
- 지도 타일 스타일, 3D 차량, 구간단속 시작/종료 핀, 지도 위 신호등·표지판
