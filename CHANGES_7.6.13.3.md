# 7.6.13.3

## D1/Worker 인프라 관련 판단
- 요청하신 대로 Cloudflare D1 + Worker 조회 API(`backend/` 폴더, `CAMERA_API_BASE`)는 이번
  배포에서 빼고, 기존처럼 경찰청 표준데이터(data.go.kr) 정적 JSON 4개 번들을 그대로 씁니다.
- 판단 근거: 이 데이터는 실시간성이 필요한 데이터가 아니라 경찰청이 몇 달~반년 단위로 갱신하는
  표(현재 번들 기준 대부분 2026-04-06/2022-12-15자)입니다. D1 + Worker + 매일 크론은 "서버에
  최신 원본을 올려두고 앱이 그때그때 내려받는" 구조일 뿐, 좌표 자체의 정확도는 원본 데이터가
  가진 오차 이상으로 좋아지지 않습니다. 반면 배포 전 상태에서도 `CAMERA_API_BASE`가 비어 있어
  실질적으로 켜져 있지도 않았고, 앱 자체를 새 버전으로 배포할 때 정적 JSON도 함께 최신화하면
  같은 효과를 훨씬 간단한 구조로 얻습니다. 그래서 이번 버전은 D1/Worker 없이, 정적 데이터 +
  더 정확한 매칭/중복 제거 로직으로 정확도를 올리는 방향으로 갔습니다.
- 나중에 "이용자가 실주행 중 확인한 좌표를 계속 누적해서 반영"하는 게 목표라면(현재
  `addUserConfirmedHoedeokCamera`처럼 코드에 직접 보정을 추가하는 방식) 그때는 누적 데이터가
  실시간으로 쌓여야 하므로 D1 같은 저장소가 실제로 의미가 있습니다. 지금 당장 필요하시면 말씀해
  주세요. 이번에는 그 용도가 아니라 "기존 정적 데이터의 매칭 정확도"가 목적이라 판단해 뺐습니다.
- app.js의 저정밀 매칭 표시(`lowPrecision`)와 안내 타이밍 여유(`tailGrace`)는 D1과 무관하게
  유용해서 그대로 유지했습니다: 경찰청 원본 좌표가 경로에서 40m 넘게 떨어져 스냅된 카메라는
  안내를 조금 더 일찍 열고, 통과 후에도 최대 60m까지 더 유지합니다.

## 세종→대전 고속화도로 구간단속 종료 후 카메라 2대 표시 — 일반화된 원인/수정
- 이전 7.6.13.1 수정은 "구간단속 종점 앞뒤 250m 이내"에 한해서만 구버전/현행 중복 레코드를
  접었습니다. 그런데 실제로 겹쳐 보이던 지점(구즉세종로 남행 종점을 지나 상서삼거리)은 종점에서
  약 900m 떨어져 있어 이 규칙에 걸리지 않았습니다.
  - 실제 데이터(경찰청 표준데이터 unmanned_traffic_cameras part2)에서 확인: '상서삼거리'라는
    같은 설치장소에 2022-12-15 대전광역시 구버전 레코드(G3122)와 2026-04-06 경찰청 현행
    레코드(K0335)가 약 170m 간격으로 각각 개별 카메라로 남아 있었습니다.
- `suppressStaleNamedDuplicateCameras()`를 새로 추가: 위치(구간단속 종점 근접 여부)와 무관하게,
  **설치장소 문구가 같은 지점을 가리키고, 도로명·제한속도가 같으며, 실제 거리가 300m 이내**이면
  구버전/비경찰청 레코드를 최신 경찰청 레코드로 흡수합니다. 설치장소 문구는 괄호·화살표·조사
  ("교차로/사거리/삼거리/네거리/합류지점/진입로/앞/후/전" 등)를 정리한 뒤 비교하므로 표기가 조금
  달라도(예: "상서삼거리" vs "상서 삼거리(→대전)") 같은 지점으로 인식합니다.
  둘 다 최신 경찰청 레코드이거나 제한속도가 다르면(실제 별개 지점일 수 있음) 손대지 않습니다.
  기존 구간단속 종점 부근 규칙(7.6.13.1)은 그대로 유지하고, 이 새 규칙을 함께 적용합니다.

## 시험
- 새 규칙 단위 시험 5개: 실제 상서삼거리 패턴(중복→1개), 표기 차이 흡수, 제한속도 다르면 유지,
  300m 넘게 떨어진 동명 지점은 유지, 둘 다 최신이면 보수적으로 유지 — 모두 통과.
- 실제 경찰청 데이터(`unmanned_traffic_cameras_part2.json`)와 K0117~상서삼거리 실좌표를 지나는
  경로로 전체 파이프라인(수집→매칭→병합→중복제거)을 끝까지 실행: 수정 전 상서삼거리 카메라
  2개(G3122, K0335) 표시 → 수정 후 1개(K0335, 최신)만 표시 확인.
- 기존 카메라/구간단속/도착/터널 회귀 시험(7.6.11.9~7.6.12.0) 전체 재실행, 모두 통과(저정밀
  타이밍 여유는 7.6.13.2에서 의도한 변경이라 그 부분만 예상대로 달라짐).
- 지역사랑상품권 마커 겹침 해소(7.6.13.0)는 온누리와 같은 구조를 그대로 재사용하고 있어 별도
  회귀 없음을 확인.
