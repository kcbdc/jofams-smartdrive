# 7.6.13.1 — 구즉세종로 구간단속 종점 직전 중복 카메라 표시 수정

## 문제
- 세종→대전 방향 고속화도로(구즉세종로)에서 구간단속(K0117 둔곡터널 전 → K0118 시알들네거리
  합류지점) 종료 지점을 통과할 때, 카메라가 2대 있는 것처럼 보임.
- 원인: 대전광역시가 2022-12-15에 등록한 구버전 개별 카메라 레코드(관리번호 G3130, "시알들네거리
  합류지점", 제한속도 80)가 경찰청의 현행 종점 레코드(K0118, 2026-04-06 갱신)와 사실상 같은
  물리적 지점(약 111m 이내, 같은 도로·같은 제한속도)을 가리키는데도 별도 관리번호를 쓰고 있어
  개별 과속카메라로 중복 표시됨.
- 기존 `suppressPostSectionDuplicateCameras`는 구간단속 종점 "직후" 220m만 중복 제거 대상으로
  봐서, 종점보다 앞쪽(약 -110m)에 찍힌 이 구버전 레코드는 걸러지지 않았음.

## 변경
- `suppressPostSectionDuplicateCameras`에 종점 직전 250m 구간에 대한 중복 제거 규칙을 추가.
  단, 최신 경찰청(2025년 이후) 개별 카메라(`currentOfficial`)는 실제로 다른 지점일 수 있으므로
  제외 대상에서 빼고, 구버전/타기관(비경찰청) 레코드에 한해서만 같은 도로·같은 제한속도일 때
  접는다. → G3130처럼 이미 경찰청 종점에 흡수된 구버전 지자체 레코드만 사라지고, 실제로 존재하는
  현행 카메라는 그대로 유지됨.

## 확인 필요
- 이번 수정은 업로드된 `sejong_daejeon_expressway_cameras.json`(우선 보정 파일) 안에서 확인 가능한
  중복(G3130) 1건만 근거로 함. 사용자가 언급한 "카메라 2대" 중 나머지 1대는 앱이 함께 불러오는
  `daejeon_sejong_corridor_cameras.json`, `unmanned_traffic_cameras_part1.json`,
  `unmanned_traffic_cameras_part2.json`(이번에 전달되지 않음) 쪽 중복일 가능성이 있음. 같은 패턴
  (구버전/비경찰청 데이터가 종점과 가까운 지점에 별도 관리번호로 남아있는 경우)이면 이번 수정으로
  함께 걸러지나, 다른 사유라면 해당 파일도 전달받아 추가 확인이 필요함.
