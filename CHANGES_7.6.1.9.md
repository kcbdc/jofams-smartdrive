# 7.6.1.9

이번 버전은 사용자가 스크린샷과 함께 신고한 5가지 문제를 수정했습니다.

- **가맹점 좌표 준-직선 패턴 추가 차단(진짜 원인 수정)**
  - 7.6.1.8에서 도입한 "좁은 경도/위도 밴드 + 넓은 반대축 스팬" 탐지 로직이 실제로는
    "전체 로드된 가맹점 수 대비 5% 이상"이라는 비율 조건도 함께 요구하고 있었다.
    그 결과 화면에 로드된 전체 가맹점 수가 많을 때(최대 1200개)는 스크린샷처럼 12~20곳 정도의
    뚜렷한 일직선이 있어도 5% 기준을 넘지 못해 걸러지지 않는 문제가 있었다.
  - 일직선 여부는 전체 데이터 규모와 무관한 순수 기하학적 특성이므로 비율 조건을 제거하고,
    절대 개수(6곳 이상) + 반대축 스팬(약 30km 이상) 조건만으로 판정하도록 수정.
  - 고정 격자 하나만 스캔하면 밴드 경계에 걸친 점이 서로 다른 bin으로 갈라져 놓칠 수 있어,
    원래 격자와 반 칸(0.01도) 밀린 격자를 함께 스캔해 겹쳐 보도록 개선.
  - 서버(`functions/api/local-voucher.js`)와 프론트(`app.js`) 양쪽에 동일하게 적용.
  - 가맹점 캐시를 v9로 갱신해 이전에 저장된 잘못된 캐시가 재사용되지 않도록 함.

- **지도를 동/서로 이동해도 가맹점이 늦게 뜨는 문제 수정**
  - 기존에는 "마지막 로드 후 45초 이내"이면 지도를 아무리 멀리 이동해도 새로 보이는 지역의
    가맹점을 다시 불러오지 않고 이전 위치 데이터를 그대로 유지하고 있었다.
  - 지도 중심이 마지막으로 불러온 위치에서 화면 반경 대비 유의미하게(약 35% 이상) 이동하면
    45초 제한과 무관하게 즉시 새로 불러오도록 수정.

- **가로 회전 시 실제 전체화면 전환**
  - 기존에는 Chrome/WebView의 "전체화면 종료 방법" 안내 팝업을 피하려고 CSS 레이아웃 확장만
    적용하고 브라우저 Fullscreen API는 쓰지 않아, 화면은 가로로 도는데 주소창 등 브라우저 UI가
    그대로 남아있는 문제가 있었다.
  - 안내 시작 버튼(`startNavigation`)과 AR 시작 버튼(`startAR`)을 누르는 실제 사용자 제스처
    컨텍스트 안에서 `requestFullscreen()`을 호출하도록 추가. 가로 회전(`orientationchange`)
    시점에도 한 번 더 시도하며, 실패해도 기존 CSS 레이아웃 확장으로 자연스럽게 대체된다.
  - 세로로 돌아오거나 주행/AR 화면을 벗어나면 자동으로 전체화면을 해제.
  - 시스템 뒤로가기 등으로 브라우저가 전체화면을 강제 종료한 경우에도 레이아웃 상태를 다시 맞춤.
  - 단, iOS Safari(아이폰)는 `<html>` 전체화면 API 자체를 지원하지 않아 기존처럼 CSS 레이아웃
    확장만 적용된다(iOS 플랫폼 제약).

- **목적지 선택 시 10초 후 안내 시작**
  - 원인: `chooseDestination()`이 경로 계산 직후 `startNavigation()`을 바로 호출해,
    `loadRouteOptions()`가 이미 시작해 둔 10초 자동 시작 카운트다운을 곧바로 취소하고
    있었다(집/회사/최근목적지/검색/지도탭 등 목적지 선택 경로 전체에 영향).
  - 즉시 시작 로직을 제거해, 경로 화면에서 10초 카운트다운이 끝나거나 사용자가 "안내 시작"을
    직접 눌렀을 때만 주행이 시작되도록 수정.

- **도보 안내 시 자동차 캐릭터 대신 일반 캐릭터로 안내**
  - 도보 경로(`routeMode==='walk'`)일 때는 지도 위 사용자 마커, 주행화면 캐릭터, AR 카메라
    화면의 캐릭터 모두 자동차 이미지 대신 캐릭터 아바타(다임/순식/훈민) 이미지를 사용하도록 통일.
  - AR에서도 동일한 로직을 공유하도록 헬퍼 함수(`characterGuideImage`)로 일원화해,
    주행 화면과 AR 화면의 캐릭터 표시가 항상 같은 규칙을 따르도록 함.

- **위성 지도 보기 추가(현재 위치 정밀 확인용)**
  - 브라우저 환경에서는 GPS 자체의 멀티위성/RTK 보정을 직접 제어할 수 없으므로, 대신 실제
    위성/항공 사진 위에 현재 위치를 겹쳐 눈으로 대조·확인할 수 있는 "위성" 보기 버튼을
    주행화면 지도 컨트롤(나침반/3D 옆)에 추가.
  - 별도 API 키가 필요 없는 Esri World Imagery 타일을 사용하며, 전환 시 경로선을 다시 그려
    정상적으로 겹쳐 보이도록 처리.
