# 7.6.13.4 — 단속카메라 위치 정확도 개선

요청하신 4단계 파이프라인(Bounding Box → 방위각 매칭 → 도로 맵매칭/법선거리 → 제동거리 기반
가변 알림) 중, 기존 코드에 이미 있던 부분(1단계 Bounding Box, 3단계 경로 투영·허용반경)은
그대로 두고, 상대적으로 느슨했던 2단계(방위각 필터)와 4단계(알림 거리)를 강화했습니다.

## 1. 방위각(Bearing) 필터 강화 — 반대편 차선 오탐 축소
- `cameraDirectionHintRaw`: 카메라 방향 값이 "숫자로 명시된 방위각"(정밀)인지 "8방위 단어
  (북/동/남서 등)에서 유도한 대략값"(45도 단위)인지 구분해 `precise` 플래그를 추가.
- `cameraDirectionCompatibleRaw` / `cameraEventDirectionCompatibleRaw`: 기존에는 정밀도
  구분 없이 "88도 초과 시 제외"라는 단일 기준만 있었습니다(반대 차선 180도는 걸러지지만,
  옆 도로·교차로처럼 40~80도 차이 나는 애매한 경우는 통과되는 경우가 있었음).
  이제는 두 단계로 나눠 적용합니다.
  - 각도차 150도 초과: 정밀도와 무관하게 항상 제외(사실상 반대 차선으로 확정).
  - 정밀 방위각: ±35도까지만 허용(요청하신 ±30도에 가깝게, 실측 오차를 감안해 소폭 여유).
  - 대략값(8방위 단어): ±60도까지 허용. 45도 단위 데이터 특성과 도로 곡률을 감안해 과거
    버그(7.6.11.8/9 — 지나치게 엄격한 필터로 실제 카메라가 통째로 사라졌던 문제)가
    재발하지 않도록 완충 폭을 남겨뒀습니다.
- `directionAwareNearestPointOnRoute`(경로 매칭 단계)의 방향 패널티 곡선에 45~70도 구간
  중간 단계(패널티 9)를 추가해, 매칭 후보 선택 시점부터 진행방향이 다른 후보를 더 낮게
  평가하도록 했습니다.

## 2. 제동거리 기반 가변 알림 거리 — 고속 구간 사전 안내거리 확대
- 기존 `cameraAlertWindowM()`은 `속도 × 13초`를 130~420m로 단순히 잘라서 썼습니다. 420m
  상한은 저속 기준으로 정해진 값이라, 고속도로(100km/h 이상)에서는 실제 반응·제동거리보다도
  짧게 끊겨 안내가 늦게 느껴지는 문제가 있었습니다.
- 다음 두 값 중 큰 쪽을 사용하도록 재작성했습니다.
  1. 속도 구간별 표준 사전 안내거리(구간 사이 선형보간): 30~50km/h → 200~300m,
     60~80km/h → 500m, 100km/h → 1,000m, 130km/h 이상 → 2,000m 상한.
  2. 반응거리(반응시간 1.8초) + 제동거리(평균 감속도 약 4.2m/s²) + 인지 여유거리(80m)로
     계산한 물리적 최소 안전거리(저속에서 구간표가 과도하게 짧아지는 것을 막는 하한선).
- 이 함수를 그대로 참조하는 안내 카드/음성 트리거(`computeSafetyCandidates`,
  `updateSafetyUI`)와 과속 경고 사전탐지(`upcomingSpeedCamera`)도 자동으로 함께 개선됩니다.

## 시험
- Node `--check`로 문법 검증 통과.
- 속도별 알림거리 시뮬레이션: 0→130m, 30→200m, 50→300m, 60~80→500m, 100→1,000m,
  130 이상→2,000m로 계획한 곡선과 일치 확인.

## 변경하지 않은 부분(이미 검증된 로직 유지)
- Bounding Box 1차 필터, 경로 투영 허용반경(70m/고속화도로 105m), 중복 카메라 제거 로직은
  이전 버전에서 이미 여러 차례 실주행 데이터로 튜닝된 값이라 이번 변경에서 건드리지 않았습니다.
