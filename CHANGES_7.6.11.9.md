# 7.6.11.9

## 도로 위 단속카메라가 전부 사라진 문제 복구 (원인: 7.6.11.6에서 들어간 코드 오류)
- 원인: `cameraDirectionHint()` 안의 `new RegExp(`(?:^|\\s|\\()${k}...`)` 가 템플릿 리터럴이라 `\s`, `\(` 의 역슬래시가 사라져
  `Invalid regular expression: /(?:^|s|()북동.../` 예외가 발생했고, 이 예외가 `loadStaticCameraEvents()` 전체를 중단시켜
  (loadSafetyEvents 가 경고만 남기고 삼킴) 공식 단속카메라 데이터가 하나도 경로에 붙지 않았음.
  (명시적 방향 필드가 있는 행만 이 코드를 우회하므로 대부분의 카메라가 영향을 받음)
- 수정: 문자열 연결 + 이중 백슬래시로 정규식 교체.
- 재발 방지:
  - 방향 판정 함수 3종은 예외를 던지지 않도록 try/catch 래퍼 적용.
  - 카메라 행 처리 루프에 행 단위 try/catch 적용(불량 행 하나가 전체를 없애지 못함).
  - `loadSafetyEvents` 후처리 4단계(중복 억제, 회덕IC 보정, 재병합, 중복 억제)도 단계별 예외 격리.
- 주소 문구(설치장소 등)에서 추정한 방향은 신뢰하지 않으므로 경로 재매칭에 쓰지 않음(명시적 단속방향 필드가 있을 때만 사용).
- 안내(카드/음성) 판정을 지도 핀 기준과 일치: 로딩 단계에서 경로에 매칭된 카메라는 34m 재검사를 하지 않음
  (기존에는 34~70m 카메라가 '핀은 보이는데 안내가 없는' 상태였음).
- 회덕IC 사용자 확인 카메라 보정이 `nearestPointOnRoute`(routeDistance 없음) 때문에 항상 조용히 무시되던 버그 수정.

## 시험 (가상 세종→대전 경로 12km, 합성 카메라 데이터, 앱 전체를 Node VM 에서 실행)
- 7.6.11.8: 표준·중복·특수문자 주소·불량 행·허용범위·구간단속 7개 시나리오 중 6개에서 카메라 0대.
- 7.6.11.9: 전 시나리오 통과 (9대 유지 / 같은 지점 4건 → 1대 / 특수문자 주소 3대 / 불량 행 섞여도 2대 / 명시 방향 필터 / 60·65m 채택·120m 제외 / 구간단속 시점·종점·일반 / 50·100km/h 주행 시 안내 시작·종료 / 회덕IC 보정 남행 1대·북행 0대).
