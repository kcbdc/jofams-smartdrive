# 7.6.2.0 변경사항

## 1) 가맹점 마커가 지도에서 한 줄로 늘어서 보이는 문제 (핵심 원인 수정)
- 원인: `styles.css`의 `.voucher-shop-marker` 규칙에 `position:relative!important`가 걸려 있었음.
  MapLibre GL은 마커 DOM 요소에 자체적으로 `position:absolute`를 적용해 지도 위 실제 좌표(픽셀)로
  배치하는데, 우리 스타일시트가 `!important`로 이를 덮어써서 `position:relative`가 되어버림.
  CSS 캐스케이드 규칙상 `!important`가 붙은 선언은 우선순위가 없는 라이브러리 기본값을 항상 이기기
  때문에, 마커가 지도 좌표가 아니라 DOM에 추가된 순서대로 세로로 쌓이는(문서 흐름) 현상이 발생함.
  화면이 좁아질수록/가맹점 수가 많을수록 이 "쌓임"이 캐릭터 바로 아래로 길게 늘어선 것처럼 더
  뚜렷하게 보였던 것.
- 수정: `.voucher-shop-marker`에서 `position:relative!important` 선언 제거. 이제 MapLibre가 지정하는
  절대 좌표 배치가 그대로 적용되어 각 가맹점이 실제 지리적 위치에 표시됨.

## 2) 축소 시 클러스터(구역별 묶음) 표시로 재편
- 화면을 축소해 넓은 지역이 보일 때는 개별 가맹점 아이콘 대신 구역별로 묶은 클러스터 배지를 표시.
  포함된 가맹점 수에 따라 large(25곳↑) / medium(8~24곳) / small(1~7곳) 세 크기로 구분.
- 클러스터링은 화면 픽셀 좌표 기준(64px 격자)으로 수행해 확대/축소 배율과 무관하게 항상 자연스럽게
  묶이도록 함. 클러스터를 탭하면 해당 구역으로 확대(fitBounds) 이동.
- 화면 폭이 1km 미만으로 확대되었을 때만 개별 가맹점 SVG 마커를 표시하도록 전환 (기존
  `voucherVisibleRadiusMeters()` 헬퍼로 현재 화면 폭 계산).
- 지도 이동/확대(`moveend`) 시 기존에도 캐시된 가맹점 데이터로 즉시 재렌더링하던 로직이 있어,
  별도 이벤트 추가 없이 줌 변경 시 클러스터 ↔ 개별 마커 전환이 자동으로 이루어짐.

## 3) '정' 글자 마커를 SVG 아이콘으로 교체
- 상습정체 구간(chronic_congestion) 마커가 '정' 한 글자 텍스트로만 표시되어 의미를 알아보기 어려웠음.
- 도로 위에 차량 두 대가 밀려 있는 형태의 SVG 아이콘(congestionMarkerSvg)으로 교체해 "정체 구간"임을
  직관적으로 알 수 있도록 개선. 기존 배경색(어두운 남색 + 노란 테두리)은 유지해 "주의" 톤을 살림.
