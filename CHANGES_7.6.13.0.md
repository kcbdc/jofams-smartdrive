# 7.6.13.0 — 지역사랑상품권 가맹점 지도 겹침 해소

## 문제
- 온누리상품권 마커는 7.6.12.0에서 화면 좌표 기준 겹침 해소를 적용했지만, 지역사랑상품권 마커는
  같은 건물 묶음 배지도 없고 화면에서 겹치는 이웃 가맹점을 옮기지도 않아 여전히 서로 겹쳐 눌리지 않는
  경우가 있었음.

## 변경 (온누리와 동일한 방식)
- **같은 건물 가맹점**: 이제 개수 배지로 묶어 표시(`voucherShopGroups`). 배지를 누르면 그 건물의
  가맹점 전체 목록(기존에 있었지만 호출되지 않던 `openVoucherBuildingList`)이 열림.
- **화면에서 서로 겹치는 가맹점**(건물이 달라도): 6곳 이하면 펼쳐서 개별 마커로, 7곳 이상이면 개수
  배지로 묶음(`clusterVoucherGroupsByOverlap`). 배지를 누르면 확대되고, 더 확대해도 분리되지 않으면
  (지점 간 거리 25m 미만이거나 확대 한계) 전체 목록이 열림.
- **화면 좌표 기준 겹침 해소**(`declutterVoucherMarkers`): 그래도 자리가 없는 마커는 가까운 빈자리로
  옮기고, 원래 위치와 가는 선·점으로 연결. 자리를 전혀 찾지 못한 마커가 2개 이상이면 그 마커들을
  다시 하나의 개수 배지로 묶음(최대 2단계 재귀). 온누리 겹침 해소에 쓰인 화면 좌표 배치 알고리즘
  (`resolveMarkerLayout`)을 그대로 재사용.
- **어디갈까 지역사랑상품권 목록**: 온누리 목록과 마찬가지로 각 항목에 "지도에서 보기" 버튼을 추가.
  누르면 목록이 닫히고 해당 위치로 확대 이동하며 마커가 주황 테두리 + 깜빡임으로 강조된 뒤 상세
  카드가 뜸(`focusVoucherItem`).
- 화면 폭이 1km 이상으로 넓게 보일 때 쓰는 기존 격자 클러스터(`clusterVoucherItemsByPixel`)는 그대로
  두되, 그 결과에도 동일한 화면 좌표 겹침 해소를 한 번 더 적용해 배지 크기 차이로 인한 경계 겹침까지
  없앰.

## 시험
- 겹침 배치 알고리즘을 실제 코드에서 그대로 뽑아 격리 단위 시험: 18개 가맹점(밀집 8곳 + 6곳 + 3곳 +
  2곳 구성) → 겹침 38쌍에서 최종 마커 11개(개별 10 + 배지 1)로 재배치, 겹침 0쌍, 원위치 표시선 7개.
  이는 온누리 마커 시험과 같은 방법(합성 데이터에 실제 CSS 마커 크기를 적용한 화면 좌표 계산)이다.
- 실제 지역사랑상품권 데이터와 폰 실기기에서는 아직 확인하지 못했다(온누리와 동일하게 확인 필요).
