# 7.6.6.1 버그 수정

## 1. 좌회전/우회전인데 "직진"으로 잘못 안내되던 문제
`laneDirectionInfo()`가 방향 판정에 쓰는 숫자 코드 배열이 화면 회전 아이콘(`turnSvg`)·회전
음성 안내(`guideVoiceCategory`)가 쓰는 기준과 서로 어긋나 있었습니다. 예를 들어 실제 우회전
(g.type=2, guidance="우회전")인 경우, 문구 기준으로는 오른쪽으로 판정되지만 숫자 배열
기준으로는 동시에 왼쪽으로도 판정되어 "왼쪽이면서 오른쪽"이 되어버렸고, 코드가 이 경우를
처리하지 못해 기본값인 "직진 차로"로 빠져 `"OOO미터 앞 직진 차로를 이용하세요"`라는
엉뚱한 음성이 나왔습니다.
→ 안내 문구(좌회전/우회전 등)를 최우선으로 신뢰하고, 문구가 없을 때만 화면 아이콘과 동일한
기준의 타입 코드로 판정하도록 수정. 이제 화면에 표시되는 회전 방향과 음성 안내가 항상
일치합니다.

## 2. 없는 구간단속을 있다고 안내하던 문제
공공데이터에는 구간단속 카메라의 "시점"과 "종점"이 각각 별도 행으로 들어있는데, 짝이 되는
시점 또는 종점을 찾지 못한 단독 행도 그대로 `section_speed_camera`(구간단속)로 등록해
"구간단속 시작 구간입니다"라는 안내와 평균속도 패널이 계속 표시되고 있었습니다. 실제로는
구간(평균속도) 단속인지 확인되지 않은 지점이었습니다.
→ 시점·종점이 정상적으로 짝지어진 경우에만 '구간단속'으로 안내하고, 짝을 찾지 못한 단독
행은 위치 정보는 유지하되 일반 지점 단속카메라(`speed_camera`)로 낮춰서 안내하도록 수정.
없는 구간단속을 있다고 안내하는 오탐이 사라집니다.

## 3. 경유지를 지났는데 다시 경유지로 되돌아가며 도착시간이 늘어나던 문제
경유지 통과 여부를 "현재 경로 geometry에서 경유지와 가장 가까운 좌표의 인덱스"만으로
판정했는데, 목적지와 경유지가 같은 도로 위에 있거나 도로가 겹치는 구간에서는 이 인덱스가
실제 통과 여부와 반대로 계산될 수 있었습니다. 목적지 근처(주차장 등)에서 GPS가 흔들려
재탐색이 걸리면, 이미 지나온 경유지가 "아직 남은 경유지"로 다시 포함되어 경유지를 다시
들렀다가 목적지로 오도록 경로가 재계산되며 도착 예정 시간이 갑자기 크게 늘어났습니다.
→ 실제 GPS 위치가 경유지 반경 45m 이내로 들어오면 경로 인덱스 계산과 무관하게 해당
경유지를 목록에서 영구적으로 제거하도록 수정(`pruneVisitedWaypoints`). 이후 어떤 재탐색이
발생해도 이미 지난 경유지로는 되돌아가지 않습니다.

## 4. 목적지 부지 안쪽(본동 건물)까지 들어가며 안내가 늦게 끝나던 문제
넓은 부지의 기관(공사·공단·대학 등)은 정문에서 실제 POI(본동 건물)까지 거리가 멀어,
직선거리 기준 도착 판정만으로는 캐릭터가 부지 안쪽까지 계속 안내를 받으며 진입하게
되는 경우가 있었습니다.
→ 경로상 남은 거리(routeRemain)가 도착 반경 이내로 줄어드는 즉시 도착 처리하도록 조건을
추가. 부지 진입 지점(문 앞)에서 더 일찍 안내가 종료되고, 본동 건물 안쪽까지 경로 안내가
이어지지 않습니다.

## 참고: 경로 이탈 방지
7.6.6.0에서 추가된 주행방향(heading) 기반 경로 판정·반대 차선 감지 로직은 이번 수정과
함께 계속 유지됩니다. 3번 항목(경유지 재포함 버그) 수정으로, 목적지 부근에서 발생하던
불필요한 재탐색으로 인한 경로 이탈성 동작도 함께 줄어듭니다.
