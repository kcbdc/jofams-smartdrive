# 7.6.12.0 — 온누리상품권 가맹점 지도 겹침 해소

## 문제
- 홈 지도에서 온누리 마커(시장·상점가 구역 라벨, 같은 위치의 점포)가 서로 겹쳐 눌러볼 수 없었음.
- 어디갈까 목록의 가맹점이 지도에서 어디인지 확인할 방법이 없었음.

## 변경
- **화면 좌표 기준 겹침 해소**(`declutterOnnuriMarkers` / `resolveMarkerLayout`)
  - 우선순위(가맹점 수/강조)가 높은 마커는 제자리, 겹치는 마커는 가까운 빈자리로 이동. 이동한 마커는 **원래 위치와 가는 선·점으로 연결**.
  - 시장·상점가 구역 라벨은 자리가 없으면 핀만 보이도록 접음(compact). 그래도 자리가 없으면 개수 배지 하나로 묶음(누르면 확대 또는 전체 목록).
- **점포 마커**: 같은 건물은 기존처럼 개수 배지, 화면에서 서로 겹치는 점포는 6곳 이하면 펼쳐서 개별 표시, 7곳 이상이면 개수 배지(누르면 확대, 더 이상 분리되지 않으면 전체 목록).
- **시장·상점가 구역 마커를 누르면** 그 구역 가맹점 **전체 목록**(가까운 순, 최대 300곳)을 표시하고 항목을 누르면 상세/길찾기.
- **어디갈까 온누리 목록**: 각 항목에 "지도에서 보기" 버튼 추가 → 해당 위치로 확대 이동하고 마커를 강조(주황 테두리 + 펄스, 겹침 배치 시 최우선)한 뒤 상세 카드 표시.
- 화면 밖 지도 로딩 시점과 무관하게 마커 크기는 DOM, 위치는 지도 투영값으로 계산.

## 시험
- 겹침 배치 알고리즘 단위 시험(구역 3개 겹침 → 0, 같은 지점 12개 → 0, 과밀 60개 예외 없음, 14개 라벨 → 일부 compact + 나머지 배지).
- 실제 CSS 로 렌더링한 가짜 지도: 마커 18개 / 겹침 49쌍 → 마커 10개 / 겹침 0쌍, 원위치 표시선 7개. 클릭 동작(구역 → 목록 8곳, 점포 → 상세, 배지 → 확대/목록, 강조) 확인.
