# 7.5.8.0 UI / 최근 목적지 / 제한속도 개선

- 빨간 제한속도 원에 현재 경로에 스냅된 전방 단속카메라 제한속도를 보조값으로 표시
- 주행기록의 `전체 삭제`, 개별 `삭제` 버튼을 컴팩트 스타일로 조정
- 홈 최근 목적지 개별 삭제 추가
- 최근 목적지 전체보기에서 개별 삭제 및 전체 삭제 추가
- `/api/recents` DELETE가 `recentId` 지정 시 개별 삭제, 미지정 시 전체 삭제하도록 확장
