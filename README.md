# 조팸스 스마트 드라이브 MVP 7.0

## 주요 개선

### 지도 로딩 안정화
- MapLibre GL JS 6.6.0을 v6 권장 방식인 ESM(`maplibre-gl.mjs`)으로 동적 로딩
- unpkg 실패 시 jsDelivr ESM으로 자동 fallback
- 기본 지도 타일을 브라우저가 외부 서버에 직접 요청하지 않고 Cloudflare Pages Function `/api/tile`을 통해 same-origin으로 제공
- `/api/tile`은 CARTO 타일을 우선 사용하고 실패 시 OpenStreetMap 타일로 fallback
- Cloudflare edge cache 및 브라우저 cache-control 적용
- Pages Function 타일 프록시 실패 시 브라우저에서 OSM → CARTO 직접 타일 fallback 유지
- 지도 진입 시 resize 및 경로 재렌더링 유지

### 캐릭터 음성
- 순식: 저음 중년 남성 보이스 우선 선택
  - rate 0.86
  - pitch 0.62
  - 성숙/저음/남성 계열 TTS 이름 우선
- 훈민: 밝은 청년 남성 보이스 우선 선택
  - rate 1.12
  - pitch 1.08
  - 젊은/밝은/남성 계열 TTS 이름 우선
- 기기에 한국어 남성 TTS가 여러 개 있으면 순식과 훈민이 서로 다른 음성을 우선 사용
- 전용 음성이 없는 기기에서는 rate/pitch 차이로 캐릭터 성격을 유지

## Cloudflare 배포
`functions/api/tile.js`가 반드시 함께 배포되어야 합니다.

배포 후 다음 주소가 이미지 타일을 반환하는지 확인할 수 있습니다.

`/api/tile?z=14&x=13985&y=6469`

Pages Functions가 정상 배포되어 있다면 HTTP 200 및 PNG 이미지가 반환됩니다.

## Android
- compileSdk 36
- targetSdk 36
- versionCode 64
- versionName 6.4.0


## MVP 6.6 지도 스타일 수정
- 기본 지도: OpenFreeMap Liberty 컬러 벡터 스타일 (`https://tiles.openfreemap.org/styles/liberty`)
- API KEY REQUIRED 워터마크를 발생시키던 기존 Carto 타일 경로 제거
- OpenFreeMap 로딩 실패 시 OpenStreetMap Standard → OSM HOT 컬러 타일 순서로 fallback
- 지도는 흑백이 아니라 도로·공원·수계·POI가 구분되는 컬러 스타일을 기본 적용


## MVP 6.6 변경
- 장소 검색 결과를 검색어 포함/정확 일치 우선으로 재정렬하고 역명 검색을 보강했습니다.
- 지도는 pitch 0의 2D 컬러 탑뷰로 고정하며 3D 건물 레이어를 숨깁니다.
- MY에 주행기록, 앱정보, 개인정보처리방침 메뉴를 추가했습니다.


## MVP 6.6.1 음성 조정
- 순식: 한국어 남성 음성 우선, 저음 중년 남성 톤(rate 0.82 / pitch 0.55)
- 훈민: 한국어 남성 음성 우선, 밝은 청년 남성 톤(rate 1.10 / pitch 0.92)
- 가능한 경우 순식과 훈민은 서로 다른 한국어 남성 음성을 선택


## MVP 6.7 안전운전 안내
- 경로상 속도위반/신호위반 단속 카메라 감지 및 500m/180m 단계 음성 안내
- 스쿨존/학교 주변 경고 배너 및 감속 음성 안내
- 지도 위 C(카메라), S(스쿨존) 안전 마커 표시
- Kakao affiliate road detail safety 정보가 있으면 우선 활용
- 보조 데이터는 `/api/safety`에서 OpenStreetMap/Overpass 데이터를 조회하며, 공개 데이터 특성상 실제 시설과 차이가 있을 수 있으므로 상용화 시 공식 안전운전 데이터 공급원으로 교체 권장


## MVP 7.0 교통정보·도로상황·주변검색 개선
- 주행 HUD에 `원활 / 서행 / 지체·혼잡 / 정체·극심한 정체` 4단계 교통상태를 표시합니다.
- 도시 일반도로 예시 기준은 40km/h 이상 원활, 20~40km/h 서행, 20km/h 미만 정체로 적용하며, 공급자의 혼잡 상태값이 있으면 `지체·혼잡`을 별도로 세분화합니다.
- Kakao 경로 road segment의 `traffic_speed / traffic_state`가 제공되면 경로선에도 초록/노랑/주황/빨강으로 교통상태를 표시합니다.
- 이동식 단속카메라, 도로공사, 교통사고 이벤트를 경로상 안전 이벤트로 통합하여 지도 마커·상단 경고·음성으로 안내합니다. Kakao affiliate road-event 정보가 있으면 우선 사용하고, OSM/Overpass는 보조 데이터로 사용합니다. 실시간 사고/이동식단속 정확도가 필요한 상용 서비스는 공식 교통정보 공급원 연동이 필요합니다.
- `주유소 / 마트 / 편의점 / 주차장 / 충전소` 등 생활편의시설 검색은 GPS가 있으면 현재 위치 거리순으로 정렬합니다. 역명·상호명처럼 고유 장소를 검색하는 경우에는 기존 정확도 우선 정렬을 유지합니다.


## MVP 7.0 주행 화면 고도화
- 오른쪽에 전방 경로의 원활/서행/지체/정체 색상 교통정보 레일 표시
- 경로 버튼: 현재 위치 이후 주요 통과지점, 남은 거리, 예상 도착시간 표시
- 검색 버튼: 안내를 종료하지 않고 운행 도중 목적지 변경 및 즉시 재탐색
- 제한속도 초과 시 화면 전체 빨간색 점멸, 현재/제한속도 표시, 음성 과속 경고
- 기존 카메라, 스쿨존, 공사, 사고, AR, 캐릭터 자동차 길안내 유지

## MVP 7.0 주행 HUD 고도화
- 주행 화면 오른쪽에 전방 경로 교통 흐름을 초록(원활) / 노랑(서행) / 주황(지체·혼잡) / 빨강(정체) 세로 바로 표시합니다.
- `경로` 버튼에서 목적지, 예상 도착시간, 남은 거리·시간과 주요 통과지점을 확인합니다.
- `검색` 버튼에서 운행 중 목적지를 변경하면 현재 GPS 위치를 기준으로 즉시 재탐색합니다.
- GPS 속도가 경로 제한속도보다 3km/h 이상 높을 때 화면 전체에 반투명 빨간 경고가 점멸하고 음성으로 과속을 알립니다.
- 모션 감소 설정에서는 점멸 대신 고정 경고를 사용합니다.

### Android 6.9
- versionCode 79
- versionName 7.0.0


## MVP 7.0 추가 기능
- 가로 회전 시 주행/AR 화면 전체 영역 사용, Android에서는 시스템 바 자동 숨김
- 야간 시간(19:00~06:00) 지도 자동 다크 표시
- 주행 중 45초 주기 실시간 경로 갱신 및 교통정보 반영
- 경로 교통상태 색상선 상시 렌더링
- Kakao road-event에 VMS/전광판 메시지가 포함되면 자동 표시·음성 안내
- 차로 데이터가 제공되면 자동 표시, 미제공 시 다음 회전 방향 보조 안내만 표시
- AR 진행 화살표를 위쪽 방향, 두꺼운 반투명 스타일로 개선
- 주행화면 빈 영역 1회 탭 시 3D / 확대 / 축소 컨트롤 표시
- 마이페이지 공지사항 메뉴 추가
