# 7.6.13.5 — 터널/DR(추정주행) 개선 3건

## 1. `onTunnelState` 실제 연결 강화 (웹 쪽)
- 기존에는 네이티브가 `active:true`를 보내면 `lastRealGpsAt`만 2.6초 앞당겨서, 다음 500ms
  주기 틱이 돌 때까지 UI/추정속도 반영이 늦었음. 이제는 신호 저하 신호를 받는 즉시
  `gpsEstimated=true` 전환 + 진입속도·칼만필터 초기화(`resetTunnelKalman`)까지 그 자리에서 처리.
- `active:false`(신호 회복) 처리를 새로 추가: 기존에는 터널 직후 정확도 70m↑ fix를 최대 8회까지
  버리는 유예 로직이 있었는데, 네이티브가 신호 회복을 먼저 알려준 경우엔 이 유예를 즉시
  해제(`postTunnelBadFixes=0`)해서 실제 위치 fix를 더 빨리 받아들이도록 함 — DR 탈출 타이밍 개선.
- **주의**: 이 저장소에는 네이티브(Swift/Kotlin) 소스가 없어서, `emitTunnelState()`가 실제로
  `window.JofamsNative.onTunnelState(...)`를 호출하도록 만드는 네이티브 쪽 배선은 이번 변경에
  포함되어 있지 않습니다. 웹 쪽 수신부(entry/exit 모두)는 이번에 완성했으니, 네이티브에서
  위성 신호 저하/회복을 감지하는 지점에 `{active:true|false}` 페이로드로 이 함수를 호출하도록
  연결하면 됩니다. 네이티브 프로젝트를 공유해 주시면 그 배선까지 이어서 하겠습니다.

## 2. 네이티브 90초 DR 캡 통일 — 웹→네이티브 값 통지까지만 구현
- 웹은 이미 "알려진 터널 구간이면 25분(1,500,000ms), 아니면 3분(180,000ms)"으로 캡을 두고
  있었음(7.6.11.3). 네이티브의 고정 90초 캡과 불일치.
- `updateTunnelRouteLock()`이 알려진 터널 구간 진입/이탈로 상태를 바꿀 때마다
  `nativePost('tunnelDrConfig',{capMs, knownTunnel})`로 네이티브에 캡 값을 통지하도록 추가.
- **주의**: 이것도 웹→네이티브로 "이 값을 쓰라"고 보내는 쪽만 구현했습니다. 네이티브가 이
  이벤트를 받아 자기 90초 하드코딩 대신 이 값을 쓰도록 바꾸는 작업은 네이티브 소스가 있어야
  가능합니다. 페이로드 규격(`type:'tunnelDrConfig', payload:{capMs:number, knownTunnel:boolean}`)
  은 확정했으니 그대로 붙이면 됩니다.

## 3. 65:35 고정 블렌딩 → 1D 칼만 필터로 교체 (완료, 웹 쪽에서 전부 처리 가능)
- `simulatedTunnelSpeedMps()`: 터널 진입속도/IMU 예측을 "예측 단계", 같은 도로 ITS 교통흐름
  속도를 "측정 단계"로 두는 표준 1차원 칼만 필터로 교체.
  - 진입 직후엔 불확실도(P)가 낮아 진입속도를 더 신뢰하고, 측정(ITS) 없이 시간이 지날수록
    불확실도가 커져서 다음 ITS 값이 들어오면 더 크게 반영됨 — 기존의 "항상 65:35" 고정 비율보다
    상황에 맞게 자연스럽게 수렴함.
  - IMU 가속도가 최근(1.2초 이내) 값이면 예측 단계에 가감속 힌트로 반영.
  - ITS 데이터가 아예 없는 구간(장거리 터널 등)에서는 예측만으로 진행하고 진입속도를 그대로
    유지(불확실도만 누적) — 이전 동작과 동일하게 안전 쪽으로 수렴.
  - 터널 진입 시점(네이티브 신호든 웹 자체 판단이든)마다 `resetTunnelKalman()`으로 필터를
    리셋하고, DR 종료·실제 GPS 복귀 시 필터 상태를 정리(`tunnelKalman=null`).

## 시험
- Node로 칼만 필터 수렴 시뮬레이션: entry=20m/s, traffic=15m/s 정체 구간 → 8초에 걸쳐
  15.3m/s로 부드럽게 수렴(첫 틱부터 급격히 튀지 않음). ITS 데이터 없는 구간(entry=25m/s,
  traffic=null) → 25m/s 그대로 유지, 불확실도만 누적 확인.
- `node --check` 문법 검증 통과.

## 다음에 필요한 것
1과 2를 실제로 마무리하려면 네이티브(iOS/Android) 프로젝트 소스가 필요합니다. 공유해 주시면
`emitTunnelState()` 호출부와 90초 캡 상수를 위 웹 쪽 규격에 맞춰 바로 고쳐드리겠습니다.
